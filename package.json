{
  "name": "fancast-cf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public --functions=./functions",
    "build": "echo 'no build step'"
  },
  "devDependencies": {
    "wrangler": "^3.79.0"
  }
}
